# Geolocation Service

This service is responsible for handling user location data.

## API Endpoints

- `POST /localisation/user/:id`: Saves the user's location.
- `GET /localisation/user/:id`: Retrieves the user's last known location.

## Running the service

The service can be run using Docker. It is part of the main `docker-compose.yml` file.
